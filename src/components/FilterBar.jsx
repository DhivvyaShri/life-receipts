export default function FilterBar({filters,setFilters}){let set=(k,v)=>setFilters({...filters,[k]:v});return <div className="filters" aria-label="Life filters">{[['source',['all','spotify','transaction','household']],['time',['all','morning','afternoon','evening','night']],['type',['all','music','food','transport','entertainment','household','health','subscription']]].map(([k,opts])=><label key={k}>{k}<select value={filters[k]} onChange={e=>set(k,e.target.value)}>{opts.map(x=><option key={x}>{x}</option>)}</select></label>)}</div>}
